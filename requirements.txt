# Quantum Random Number Generator - runtime deps for the Streamlit demo (app.py).
# Kept minimal so the cloud build is lean and does NOT install qiskit-aer, whose
# C++ extension segfaults in Streamlit Community Cloud's sandbox. The app uses
# Qiskit's pure-Python StatevectorSampler instead.
#
# For the full local pipeline (noise models, full NIST battery, IBM hardware,
# plots, tests) install requirements-dev.txt instead.
#
# NOTE: requires Python 3.13 locally (qiskit-aer has no 3.14 wheels); the cloud
# app itself works on any Python with qiskit wheels.

streamlit>=1.30
qiskit>=2.4,<3
numpy>=1.26
scipy>=1.11
cryptography>=42
